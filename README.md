These images have been built and tested on docker amd64, arm32v7 and arm64v8. This is a multi platform image.

## Usage ##

    docker run -d --init -p 4046:4046/tcp yhaenggi/mednafen-server:0.5.2

## Build ##

Install/run a buildx instance, then:

    ./build.sh

## Tags ##
   * 0.5.2
